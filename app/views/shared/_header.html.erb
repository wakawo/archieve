 <nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navigation">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">Archieve</a>
    </div>
    <div class="collapse navbar-collapse" id="navigation">
      <% if user_signed_in? %>
        <%= link_to 'Sign out', destroy_user_session_path, method: :delete, class: 'btn btn-info navbar-btn navbar-right' %>
        <p class="navbar-text navbar-right username"><%= current_user.name %></p>
      <% else %>
        <%= link_to 'Sign in', new_user_session_path, class: 'btn btn-info navbar-btn navbar-right' %>
      <% end %>
    </div>
  </div>
</nav>